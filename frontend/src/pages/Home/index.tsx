import React from 'react';

import { Container } from '../../styles/home';
import Menu from '../../components/Menu';

const Home: React.FC = () => {
  return (
    <Container>
      <Menu />
    </Container>
  );
};

export default Home;
